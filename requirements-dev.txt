# ============================================
# Dependências de Desenvolvimento
# Instalar: pip install -r requirements-dev.txt
# ============================================

# ----- Testes -----
pytest>=9.0.3,<10.0
pytest-cov>=7.1.0
pytest-flask>=1.3
pytest-mock>=3.12
pytest-xdist>=3.5          # paralelização (-n auto)
pytest-timeout>=2.2        # timeout por teste
respx>=0.21.0              # mock httpx para testes de integração

# ----- Lint & Format (substituem black+isort+flake8) -----
ruff>=0.6.0                # linter + formatter ultra-rápido

# ----- Type Checking -----
mypy>=1.10
types-requests
types-python-dateutil

# ----- Security -----
bandit[toml]>=1.7          # análise estática de segurança
pip-audit>=2.7             # auditoria de vulnerabilidades (substitui safety)

# ----- Pre-commit -----
pre-commit>=3.7

# ----- Dev Utils -----
ipython>=8.20              # REPL melhorado
ipdb>=0.13                 # debugger interativo
types-PyYAML
